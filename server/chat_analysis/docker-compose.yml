version: '3'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always
    volumes:
      - ./app:/app/app # <- 이건 기존에 있던 코드 연결용
      # --- ▼▼▼ 이 "창문" 한 줄을 추가하세요 ▼▼▼ ---
      - /home/jby/onedrive_sync:/app/static_files
      # --- ▲▲▲ 여기까지 추가 ▲▲▲ ---